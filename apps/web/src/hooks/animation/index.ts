/**
 * Animation Hooks
 * 
 * Reusable hooks for common animation patterns used throughout the app.
 * These hooks extract complex animation logic from components for better
 * reusability, testability, and separation of concerns.
 */

export { useReducedMotion } from './useReducedMotion';
export { useSmoothScroll } from './useSmoothScroll';
export { useScrollFade } from './useScrollFade';